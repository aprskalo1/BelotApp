@page
@model LoginModel

<div class="d-flex align-items-center justify-content-center" style="min-height: 80vh;">
    <div class="d-flex justify-content-center w-100">
        <div class="w-100 p-4 shadow-sm" style="max-width: 30rem; border-radius: 20px; background-color: white;">
            <div class="title mb-3">Prijava</div>
            <section>
                <form id="account" method="post">
                    <div asp-validation-summary="ModelOnly" class="text-danger" role="alert"></div>
                    <div class="form-floating mb-3">
                        <input asp-for="Input.Email" class="form-control" autocomplete="username" aria-required="true" placeholder="name@example.com" />
                        <label asp-for="Input.Email" class="form-label">Email</label>
                        <span asp-validation-for="Input.Email" class="text-danger"></span>
                    </div>
                    <div class="form-floating mb-3">
                        <input asp-for="Input.Password" class="form-control" autocomplete="current-password" aria-required="true" placeholder="password" />
                        <label asp-for="Input.Password" class="form-label">Lozinka</label>
                        <span asp-validation-for="Input.Password" class="text-danger"></span>
                    </div>
                    <div>
                        <p class="d-flex justify-content-between">
                            <a id="forgot-password" class="text-decoration-none text-secondary" asp-page="./ForgotPassword">Zaboravili ste lozinku?</a>
                            <a id="resend-confirmation" class="text-decoration-none text-secondary" asp-page="./ResendEmailConfirmation">Potvrdi račun</a>

                        </p>
                    </div>
                    <div>
                        <button id="login-submit" type="submit" class="w-100 btn btn-lg btn-primary mb-2">Prijavi se</button>
                    </div>
                </form>
            </section>

            <div class="text-center position-relative">
                <hr class="mx-auto w-75">
                <span class="position-absolute top-50 start-50 translate-middle ps-2 pe-2" style="background: white;">ILI</span>
            </div>

            <section class="mt-2 mb-1">
                @{
                    if ((Model.ExternalLogins?.Count ?? 0) == 0)
                    {
                        <div>
                            <p>
                                Nažalost ne postoje vanjske usluge prijave.
                            </p>
                        </div>
                    }
                    else
                    {
                        <form id="external-account" asp-page="./ExternalLogin" asp-route-returnUrl="@Model.ReturnUrl" method="post" class="form-horizontal">
                            <div>
                                <p>
                                    @foreach (var provider in Model.ExternalLogins!)
                                    {
                                        <button type="submit" class="w-100 btn btn-lg btn-light shadow-sm d-flex justify-content-around align-items-center text-secondary" name="provider" value="@provider.Name" title="Log in using your @provider.DisplayName account"><i class="fa-brands fa-google" style="color: #0d6efd;"></i> NASTAVI S GOOGLE RAČUNOM</button>
                                    }
                                </p>
                            </div>
                        </form>
                    }
                }
            </section>
            <section class="d-flex justify-content-center align-items-center mt-3">
                <span class="d-flex flex-column">
                    <a asp-page="./Register" class="text-decoration-none text-secondary text-center custom-text-shadow" asp-route-returnUrl="@Model.ReturnUrl">Registriraj se</a>
                </span>
            </section>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
